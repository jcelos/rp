// João Celos

/**
 * EXERCÍCIO 3 - RESTRIÇÕES ESTRUTURAIS DE UMA ÁRVORE BINÁRIA DE BUSCA
 * 
 * - Cada nó tem no máximo dois filhos: esquerdo e direito.
 * - Para qualquer nó N, todos os valores no subárvore esquerda são menores que N.valor.
 * - Para qualquer nó N, todos os valores no subárvore direita são maiores que N.valor.
 * - Não há duplicatas (às vezes duplicatas são permitidas com convenções, mas regra clássica: não).
 * - A estrutura é recursiva: subárvores também são BSTs.
 * - Operações típicas assumem ordenação rígida: inserção, busca, remoção dependem dessa propriedade.
 * - Balanceamento não é obrigatório, mas afeta complexidade (AVL, Red-Black, etc.).
 * - Complexidade média: O(log n) para busca/insersão em árvores balanceadas; O(n) no pior caso (degenerada).
 */

console.log('Arquivo de referência com as restrições estruturais de uma BST.');
export {};
